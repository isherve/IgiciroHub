# Point the Expo app at your running Django backend.
# For a physical device on the same Wi-Fi, use your machine's LAN IP.
# For the Android emulator, 10.0.2.2 maps to the host's localhost (default).
# For iOS simulator / web, 127.0.0.1 is used by default.
#
# EXPO_PUBLIC_API_URL=http://192.168.1.20:8000
